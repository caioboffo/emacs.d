;; setup-powerline.el --- Powerline configuration

;;; Commentary:

;;; Code:

(use-package powerline
  :ensure t
  :config
  (setq powerline-default-separator 'arrow)
  (powerline-default-theme))

(provide 'setup-powerline)
;;; setup-powerline.el ends here
